<div class="topbar">
    <div class="topbar-inner">
        <div class="container">
            <a class="brand" href="{{ path('LansoleProjectBundle_homepage') }}">Lansole Toolkit</a>
            {% block navigation %}
                {{ knp_menu_render('LansoleProjectBundle:Builder:mainMenu', { 'currentClass': 'active' }) }}

                {{ knp_menu_render('LansoleProjectBundle:Builder:authenticationMenu', { 'currentClass': 'active' }) }}
            {% endblock %}
        </div>
    </div>
</div>